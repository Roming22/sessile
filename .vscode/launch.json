{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Build firmware",
      "type": "node-terminal",
      "request": "launch",
      "command": "cargo make uf2 --release",
      "cwd": "${workspaceFolder}/rmk"
    }
  ]
}

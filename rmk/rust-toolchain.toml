[toolchain]
# Pin below 1.97: core::pin::pin!(slice.as_mut_slice()) type-checks as
# Pin<&mut &mut [T]> on 1.97+, which breaks rmk 0.8.2 (async_matrix).
channel = "1.96.0"
components = ["rust-src", "rustfmt", "llvm-tools"]
targets = ["thumbv7em-none-eabihf"]
